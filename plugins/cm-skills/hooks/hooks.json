{
  "description": "Force PR reviews/approvals/merges through the Maestro chartmetric-claude GitHub App instead of the caller's personal gh identity.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/require-maestro-review.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
